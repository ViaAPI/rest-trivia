title: UserPassword.v1
type: object
description: This model is used only when the user's passwords need to be revalidated.
examples:
  - password: pa$$word
properties:
  password:
    type: string
    description: Preferably a hashed value.
    format: password
required:
  - password
